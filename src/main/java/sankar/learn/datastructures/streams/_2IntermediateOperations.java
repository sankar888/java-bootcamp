package sankar.learn.datastructures.streams;

import org.junit.jupiter.api.Test;

import java.util.Random;
import java.util.stream.IntStream;

/**
 * Stateless Intermediate Operations:
 * Stateless operations, such as filter and map, retain no state from previously seen element when processing a new element -- each element can be processed independently of operations on other elements.
 * Refer the following classes for the stateful intermediate operations
 * https://docs.oracle.com/javase/8/docs/api/java/util/stream/BaseStream.html
 * https://docs.oracle.com/javase/8/docs/api/java/util/stream/Stream.html
 */
public class _2IntermediateOperations {

    //TODO: know about the advantages of lazily processing stream

    @Test
    public void testShortCircuitingOperation() {

    }
}
